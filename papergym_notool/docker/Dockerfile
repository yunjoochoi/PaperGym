# The no-tool Stage 1 baseline runs locally.
#
# This placeholder is kept only to avoid breaking old references to
# papergym_notool/docker/Dockerfile. Reproduction should use:
#
#   uv run python scripts/run_accumulator.py --help

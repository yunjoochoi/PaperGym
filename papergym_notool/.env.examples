LITELLM_MODEL=gpt-5
EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=sk-...
OPENAI_API_BASE=https://api.openai.com/v1

# Semantic Scholar bulk-search key (used by scripts/bootstrap_papers.py).
# Optional; without a key, S2 throttles to ~100 reqs / 5 min.
S2_API_KEY=s2k-...
